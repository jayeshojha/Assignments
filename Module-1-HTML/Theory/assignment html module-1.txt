assignment html module-1

1. Are the HTML tags and elements the same thing?
ans:- 
      Html tags and elements are complitly diffrent.html tags are use to hold contents there are many diffrent-diffrent tags every tag has diffrent work.
these tags are:-
<h1></h1>,<br>,<img>.
html elements are components that are used in html page.html element is defined by a start tag(<>) then content then closing tag (</>).
for eg.:- <h1> this is assignment </h1>
           

2. What are tags and attributes in HTML?
ans:-
      tags:- 
             html tags are keywords that which defines that how web browser will format and display content.there are two types of tag paired tag and unpaired tag.
paired tags:-
              these tags have start tag (<>) and closing tag(</>) between thses two tags we write content,
              for eg:- <h1> </h1>, <p1> </p1>,<div> </div>

unpaired tags:-
               these type of tags have single tag that is also called self closing tags.
               for eg:-
                        <br>,<img>
attributes:-
             attribute are use to define property of tag that is used to adjust the behavior or dispaly of html element.
              for eg:-
                       height,color,bgcolor,width etc.


3. What are void elements in HTML?
ans:- 
      void element is self closing tag that have not closing tag.this elements have attributes and this element not have content.
      this element not have closing tag.
for eg:-
         <br>,<hr>,<input>


4. What are HTML Entities?
ans:-
       html entities are used to replecement of a reserved character.these charecter are replaced because some character are reserved in html.
       for exa:-
                if we use lessthen(<) and greterthen(>) symbol the browser can mix them with tags that's why we used character entities in html to display
                reserved character in html.
        there are some entites we use in html:-
                                                lessthen(<) = &lt;
                                                greterthen(>) = &gt;
                                                ampersand(&) = &amp;
        
     

5. What are different types of lists in HTML?
ans:-
      there are three difrent types of listL:-
    1. orderd list (ol)
    2. unorderd list (ul)
    3. description list(dl)


6. What is the ‘class’ attribute in HTML?
ans:-
      the class is attribute that define one or more class name for elments.class name can be used by css or javascript.
for eg:-
         <h2 class="head"> this is heading </h1>

  
7. What is the difference between the ‘id’ attribute and the ‘class’ attribute of HTML elements?
ans:-
       id attribute:- 
                      id attributes give diffrent or uniqe name for elements every elements have their own uniqe name that is given by id attribute. we can  not give same name for every elements in id attribute.

       class attribute:-
                        class attributes give same name and more than one name for every elements.that is used by css and javascript.every elements have 
                         more than one class and same name.


8. What are the various formatting tags in HTML?
ans:-
      there are various formatting tag in html these tags are:-
                                                                <b>	        This is a physical tag, which is used to bold the text written between it.
                                                                <strong>	This is a logical tag, which tells the browser that the text is important.
                                                                <i>	        This is a physical tag which is used to make text italic.
                                                                <mark>	        This tag is used to highlight text.
                                                                <u>	        This tag is used to underline text written between it.


9. How is Cell Padding different from Cell Spacing?
ans:-
     cell padding:-
                   Cell padding specifies the space between the border of a table cell and its contents it defines the whitespace between the cell edge and the content of the cell.

cell spacing:-
               Cellspacing specifies the space between cells it defines the whitespace between the edges of the adjacent cells.


10. How can we club two or more rows or columns into a single row or column in an HTML
table?
ans:-
      we can club two or more rows or columns into a single row or colmuns by using rowspan and colspan attributes.
    for eg.:-
                 <table border="1">
        <tr>
            <th colspan="2">1</th>
        </tr>
        <tr>
            <td>2</td>
            <td>3</td>
        </tr>
     </table>


11. What is the difference between a block-level element and an inline element?
ans:-
     block level elements:-
                           a block level elements takes full width of block and its always start with new line.we can use inline elemnets in block level elements.
               for eg:-
                         <div>this is container <p>this is para</p> <div>
     inline elements:-
                         a inline elements takes only needed width.an inline element does not start on a new line.we can not use block level elements in              inline elements     
                   for eg:-
                            <p> this is para <div> this is container</div> its take a new line </p>
                    
                        this is not rigth way to use this elements.
            
      


12. How to create a Hyperlink in HTML?
ans:-
      by using anchor tag (<a></a>) we can create hyper link in html.we can set url in herf attribute after that our hype link is created.


13. What is the use of an iframe tag?
ans:-
       Iframe is used to display a nested webpage . The HTML <iframe> tag defines an inline frame,that's why it is also called as an Inline frame.
  iframe embeds another document within the current HTML document in the rectangular region.
                 syntax:-  
                           <iframe src="URL"></iframe> 


14. What is the use of a span tag? Explain with example?
ans:- 
       The span tag is used for the grouping of inline elements and this tag does not make any visual change by itself. span is very similar to the div tag, but div is a block-level tag and span is an inline tag.
              syntax:-
                         <span class="">Some Text</span> 
              <span style="background-color: lightgreen;"> this is span </span>.

      

15. How to insert a picture into a background image of a web page?
ans:- 
       by given background attribute in body tag (<body>) and give url of img. in background attribute
  for eg:-
            <body background = "URL or path" > Website Body </body>

         
16. How are active links different from normal links?
ans:- 
    active links:
                active link are that links that open live web pages that are working on live server.
    
    normal links:-
                   This is just a line of code containing a pointer to another resource.but this link are functionly broken
    
      
17. What are the different tags to separate sections of text?
ans:-
       there are three type of tags to seprate section of text:-
     1. <br>  (break row)
     2. <div> (container or box)
     3  <p>    (paragraph)

         
18. What is SVG?
ans:-
      SVG stands for Scalable Vector Graphics.the HTML <svg> element is a container for SVG graphics.
      SVG has several methods for drawing paths, boxes, circles, text, and graphic images.


19. What is difference between HTML and XHTML?
ans:-
        html:-
              Hypertext Markup Language is a markup language used to create web pages and other information that can be displayed in a web browser.
HTML allows for the use of deprecated attributes.


Xhtml:-
        eXtensible Hypertext Markup Language is a markup language that is a stricter version of HTML and conforms to XML syntax.XHTML requires end tags for all elements and attributes to be quoted.XHTML does not allow the use of deprecated attributes and requires all attributes to be lowercase. 
                                                              

20. What are logical and physical tags in HTML?
ans:-
      physical tags:-
                      Physical Tags are used to indicate that how specific characters are to be formatted or indicated using HTML tags. Any physical tag may contain any item allowed in text, including conventional text, images, line breaks, etc. All physical tags require ending tags.

logical tags:-
               Logical tags are used to tell the browser what kind of text is written inside the tags. They are different from physical tags because physical tags are used to decide the appearance of the text and do not provide any information about the text.
                     